import React from 'react';
import PropTypes from 'prop-types';

const Button = props => {
    return (
        <div>
            
        </div>
    );
};

Button.propTypes = {
    filled: PropTypes.bool,
};

export default Button;